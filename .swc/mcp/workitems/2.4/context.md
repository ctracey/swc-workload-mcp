## Pass 1 — 2026-05-25

- **Decision:** Promoted `bridge._resolve_binary` to public `bridge.resolve_binary` (same signature, same `CLINotFoundError` semantics). Updated the single internal call site in `bridge.invoke`. `__all__` now exports `resolve_binary`. Server reuses this from a try/except in `main()` — satisfies REQ-07 with no duplicated `shutil.which` lookup.
- **Decision:** Tool registration uses `mcp.add_tool(fn)` (no name override — FastMCP picks up the function name automatically). The functional decorator form `mcp.tool()(fn)` would have worked too; `add_tool` is more idiomatic and matches the SDK's documented public API. Loop over `tools.TOOLS`, satisfying REQ-03 (no inline per-op knowledge).
- **Decision:** Constructed `FastMCP("swc-workload")` at module-import time and ran `_register_tools()` at import time too. This keeps `main()` minimal (presence check + `mcp.run`) and lets unit tests inspect the registered tools without invoking `main()` at all (so the `mcp.run` stdio loop is never reached in those tests). `mcp.run("stdio")` is the last line of `main()`.
- **Decision:** Startup check runs BEFORE `mcp.run()` — and explicitly NOT inside any callback registered with FastMCP. This makes "`mcp.run()` is NOT invoked on missing CLI" trivially true (the `raise SystemExit(1)` happens before that line is reached). The check does NOT prevent module import — the FastMCP instance and registered tools both exist after import; only the live transport requires the CLI.
- **Decision:** Fail-fast message uses `raise SystemExit(1)` rather than `sys.exit(1)` so test code can catch it cleanly via `pytest.raises(SystemExit)`. Behaviour is identical from a process-exit perspective.
- **Assumption:** REQ-09 smoke test uses `mcp.shared.memory.create_connected_server_and_client_session(server.mcp._mcp_server)` — passes the low-level `Server` instance (accessed via `_mcp_server` on `FastMCP`), per the quality-baseline hint. The `_mcp_server` attribute is the underlying SDK `Server`; the helper accepts either `Server` or `FastMCP` per its signature. Verified by passing test.
- **Decision:** Used `anyio`'s pytest plugin (already installed transitively via `mcp`) instead of adding `pytest-asyncio` as a dev dep. The `@pytest.mark.anyio` marker plus an `anyio_backend` fixture returning `"asyncio"` is enough. Avoids growing the dep list for one async test.
- **Decision:** Captured the real `swc-workload` path at module import time (`_REAL_CLI_PATH = shutil.which("swc-workload")`) in `test_server.py`, then point `SWC_WORKLOAD_BIN` at it inside the smoke test. The autouse `_isolate_env` fixture wipes `PATH` before each test runs, so the smoke test would otherwise not find the CLI even if it's installed. Capturing pre-fixture is the simplest route to "fail loud if missing, otherwise use the real one".
- **Decision:** REQ-03 static check excludes `"list"` from the forbidden literal-string set — too generic, would false-positive on any docstring mentioning lists. The positive check (`"TOOLS" in source`) catches the regression in practice — if someone enumerates tools by name, they would have to mention all twelve, including `init`, `add`, etc., which are unique enough to flag.
- **Decision:** Created `docs/architecture.md` per solution.md's "additional deliverables". Five sections as specified — what this is, what is MCP, layers at a glance (ASCII diagram), each layer in detail (server / tool / bridge / CLI), startup behaviour. Tone is technical reference, ~1 page. README (work item 4) will link to it.
- **Decision:** Replaced "graceful degradation" wording in `.swc/mcp/architecture.md` and `.swc/mcp/notes.md` with fail-fast description per REQ-08. Kept the rest of those docs intact.
- **Tried (and kept):** `server.mcp._tool_manager.list_tools()` to enumerate registered tools in unit tests. The public `mcp.list_tools()` is async (returns a coroutine); the internal `_tool_manager.list_tools()` is sync and returns the same tool objects. Used in the REQ-01 and REQ-05 tests where awaiting would add complexity for no behavioural difference.

## Pass 2 — 2026-05-25

- **Decision (F-03):** Tightened the REQ-01 happy-path test from `expected.issubset(registered)` to `registered == expected`. The original `issubset` form would have silently allowed leaked test helpers or duplicate registrations; equality fails loud on any extra. REQ-05's exactly-12 test already enforced this, so the change is purely about making REQ-01 read as the strict wiring check it should be. Also tweaked the inline comment to flag the bidirectional intent ("no extras leaked in, no entries dropped").
- **Decision (F-04):** Replaced the weak `assert "TOOLS" in src_no_doc` positive guard in the REQ-03 test with a regex match against the actual loop pattern `for\s+\w+\s+in\s+tools\.TOOLS`. The bare-token check would have passed for any module merely *mentioning* the identifier (e.g. in a docstring); the regex demands the registration loop is actually present in source. Added a `re` import to the test module to support this. Verified the regex matches the existing `for fn in tools.TOOLS:` in `server.py`.
- **Tried:** Considered just adding `"for fn in tools.TOOLS" in src_no_doc` as a literal substring check. Rejected in favour of the regex — `\s+\w+\s+` keeps the assertion robust to a renamed loop variable (e.g. `for tool in tools.TOOLS:`) without weakening the intent. The loop-variable name is implementation detail; what matters is that `tools.TOOLS` is iterated.
- **Test results:** All 57 tests pass (9 bridge + 10 server + 38 tools). REQ-01 and REQ-03 tests both pass under the tighter assertions.
